apiVersion: apps/v1
kind: Deployment
metadata:
  name: forge-worker
  namespace: forge
spec:
  replicas: 7
  selector:
    matchLabels: { app: forge-worker }
  template:
    metadata:
      labels: { app: forge-worker }
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "9100"
    spec:
      # Grace period > FORGE_DRAIN_TIMEOUT (25s): a rolling update lets
      # in-flight jobs finish instead of leaning on the reaper. SIGKILL
      # after this window is survivable by design -- that's what the
      # lease/reaper machinery is for -- but graceful is cheaper.
      terminationGracePeriodSeconds: 40
      containers:
        - name: worker
          image: forge-worker:latest # --build-arg BINARY=forge-worker
          ports:
            - { containerPort: 9100, name: metrics }
          envFrom:
            - configMapRef: { name: forge-config }
          env:
            - name: FORGE_DATABASE_URL
              valueFrom: { secretKeyRef: { name: forge-secrets, key: database-url } }
            - name: FORGE_CALLBACK_SECRET
              valueFrom: { secretKeyRef: { name: forge-secrets, key: callback-secret, optional: true } }
            # Worker identity from the pod name: unique, stable for the
            # pod's life, and legible in `forgectl live` output.
            - name: FORGE_WORKER_ID
              valueFrom: { fieldRef: { fieldPath: metadata.name } }
          livenessProbe:
            httpGet: { path: /healthz, port: metrics }
            periodSeconds: 10
          resources:
            requests: { cpu: 500m, memory: 128Mi }
            limits: { memory: 512Mi }
