# Forge

A distributed job-processing platform in Go: Redis Streams as the
transport, PostgreSQL as the source of truth, a bounded worker pool with
graceful drain, retries with full-jitter backoff, a dead-letter queue, and
benchmarks that measure what the README claims.

Under active development — this README grows as the pieces land.
